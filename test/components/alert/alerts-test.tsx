/*
import * as React from "react";
import {expect,assert} from 'chai'
import {shallow} from 'enzyme'

import {DWAlertApp} from '../../../src/components/alert/alertComponent'
import {DWAlerts} from '../../../src/components/alert/modle'

describe("<AlertS/>",()=>{

    let dwAlerts : DWAlerts;

    beforeEach(() => {
        dwAlerts = new DWAlerts();
    });

    it("should render correctly", () => {
        const wrapper = shallow(<DWAlertApp alerts={dwAlerts}  />)
    });
})
*/